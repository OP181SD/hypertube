# Retain npm v6 peer dependency resolution behavior.
# Required because eslint-plugin-react-hooks@7 has not yet declared
# eslint v10 support in its peerDependencies, even though it works fine.
legacy-peer-deps=true
